name: iOS Build Check

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build:
    runs-on: macos-15

    steps:
      - name: Checkout (with submodules)
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Fix ThirdParty submodules (strip main.swift / nested Package.swift)
        run: ./scripts/fix-thirdparty-submodules.sh

           - name: Select Xcode version
        run: sudo xcode-select -s /Applications/Xcode_16.2.app/Contents/Developer

      - name: Wake Up Headless iOS Simulator Daemon
        # Fixes the GitHub Actions "Unable to connect to simulator" bug by warming up the background process.
        run: |
          xcrun simctl list > /dev/null
          sleep 5

      - name: Install Missing iOS 18.2 Platform Runtime
        run: xcodebuild -downloadPlatform iOS -buildVersion 18.2

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate Xcode project from project.yml
        run: xcodegen generate

      - name: List schemes (sanity check)
        run: xcodebuild -list -project OffGrid.xcodeproj

      - name: Build for iOS Simulator
        run: |
          set -o pipefail
          xcodebuild build \
            -project OffGrid.xcodeproj \
            -scheme OffGrid \
            -sdk iphonesimulator \
            -destination "generic/platform=iOS Simulator" \
            CODE_SIGNING_ALLOWED=NO \
            ONLY_ACTIVE_ARCH=YES \
            ARCHS="arm64" \
            COMPILER_INDEX_STORE_ENABLE=NO
